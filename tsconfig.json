{
    "extends": "./packages/pyright/tsconfig.json",
    "compilerOptions": {
        "outDir": "./out",
        "baseUrl": "build",
        "paths": {
            "*": ["types/*"]
        }
    },
    "include": ["build/**/*.ts", "build/**/*.js"],
    "exclude": ["node_modules", "dist", "out"]
}
